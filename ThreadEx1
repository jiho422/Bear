package com.thread;

public class ThreadEx1 extends Thread {

	@Override
	public void run() {		//스레드를 만드는 방법 첫번째, run메소드가 없으면 스레드를 쓸 수 없기 때문에 반드시 run 메소드를 오버라이딩 해야한다.
		for(int i=0;i<5;i++) {
			System.out.println(getName());		//run 메소드의 이름 정해주기
		}
	}
}
